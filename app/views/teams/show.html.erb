<div class="center hero-unit">
  <% provide(:title, @team.team) %>

  <h1>
    <%= @team.team %>
  </h1>
  <h2>
    The <a href= <%= @team.url %> ><%= @team.team %></a>
    are a team in the National Football League.  They play in the <%= @team.division %> Division.
  <%= if @team.user != nil; "This team is coached by " + @team.user.name; end %>

  </h2>
  <h2>
    Click <a href=/teams>here</a> to see a full list of all NFL teams.
  </h2>
</div>


<%= link_to image_tag((@team.abbrev.to_s + ".png"), alt: "NFL"), @team.url %>




<h2>
  Most recent game score: <%= get_score @team %>
</h2>